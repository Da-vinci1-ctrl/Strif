# Strif

Strif is an experimental programming language focused on agents, execution flow, and VM-based execution.

## Install (development)

```bash
git clone https://github.com/Da-vinci1-ctrl/strif-lang.git
cd strif-lang
pip install -e .
